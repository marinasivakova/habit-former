export const ROUTES = {
  OVERVIEW: "/",
  HABIT_LIST: "/habit",
  HABIT_DETAIL: "/habit/:id",
  AUTH: "/auth",
  HISTORY: "/history",
  PROFILE: "/profile",
  // add future routes here
};

export type TRoutes = typeof ROUTES;
